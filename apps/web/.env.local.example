# Public — safe to expose to the browser (protected by Row Level Security).
NEXT_PUBLIC_SUPABASE_URL=https://rtrjhxtbldyaeltjikzg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_NezQA3NhB2qDB3eOc8rAWQ_6xheczTd
# Used to build the email-confirmation redirect link — update for production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Secret — server-only. Get from Supabase dashboard > Project Settings > API.
# Bypasses RLS: never expose to the client or commit the real value.
SUPABASE_SERVICE_ROLE_KEY=

# Payment gateways — secret keys, server-only. No merchant account was
# confirmed at setup time, so the payments package stays gateway-agnostic;
# set DEFAULT_PAYMENT_GATEWAY once one is chosen.
DEFAULT_PAYMENT_GATEWAY=paystack
PAYSTACK_SECRET_KEY=
FLUTTERWAVE_SECRET_KEY=
FLUTTERWAVE_WEBHOOK_SECRET_HASH=

# Notifications (see CLAUDE.md section 7)
TERMII_API_KEY=
RESEND_API_KEY=
