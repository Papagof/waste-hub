# The web app (Next.js, React 19) and mobile app (Expo, React 18) are
# independent bundles that never share a runtime, so npm's strict
# cross-workspace peer resolution is a false conflict here.
legacy-peer-deps=true
